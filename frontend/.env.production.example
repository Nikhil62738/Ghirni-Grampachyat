# Frontend production environment (set these in Netlify, NOT in this file).
# Netlify -> Site configuration -> Environment variables.
#
# Must point at your deployed Render backend and INCLUDE the /api suffix.
# Example:
VITE_API_BASE_URL=https://gp-ghirni-tax-api.onrender.com/api

Place the built Android app here as: gp-ghirni-tax.apk

The website download popup links to /downloads/gp-ghirni-tax.apk by default,
or to the URL set in the Netlify env var VITE_APP_DOWNLOAD_URL.

To build the APK, see ../../../mobile/README.md (EAS build instructions).
